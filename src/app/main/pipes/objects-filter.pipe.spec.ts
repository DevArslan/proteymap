import { ObjectsFilterPipe } from './objects-filter.pipe';

describe('ObjectsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
